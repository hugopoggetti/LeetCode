#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int** generate(int numRows, int* returnSize, int** returnColumnSizes)
{
    return NULL;
}

int main(void)
{
    int size = 0;;
    int *size2 = {0};
    int **ge = generate(1, &size, &size2);
}